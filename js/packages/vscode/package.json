{
  "name": "zodra-vscode",
  "displayName": "Zodra",
  "description": "VS Code support for Zodra type system DSL",
  "version": "0.1.0",
  "publisher": "zodra",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Programming Languages"],
  "activationEvents": [
    "workspaceContains:**/zodra.gemspec",
    "workspaceContains:**/app/types/**/*.rb"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "zodra.openGeneratedTS",
        "title": "Zodra: Open Generated TypeScript"
      },
      {
        "command": "zodra.openRubySource",
        "title": "Zodra: Open Ruby Source"
      },
      {
        "command": "zodra.export",
        "title": "Zodra: Export TypeScript"
      }
    ],
    "configuration": {
      "title": "Zodra",
      "properties": {
        "zodra.autoExport": {
          "type": "boolean",
          "default": true,
          "description": "Automatically run zodra:export when Ruby definitions change"
        },
        "zodra.outputPath": {
          "type": "string",
          "default": "app/javascript/types",
          "description": "Path to generated TypeScript files (relative to workspace root)"
        },
        "zodra.exportCommand": {
          "type": "string",
          "default": "bundle exec rails zodra:export",
          "description": "Shell command to run zodra:export (e.g. 'rbenv exec bundle exec rails zodra:export')"
        },
        "zodra.exportDebounceMs": {
          "type": "number",
          "default": 1500,
          "description": "Debounce delay for auto-export (milliseconds)"
        },
        "zodra.sourcePaths": {
          "type": "array",
          "items": { "type": "string" },
          "default": ["app/types", "app/contracts", "config/apis"],
          "description": "Paths to Zodra Ruby source directories (relative to workspace root)"
        },
        "zodra.staleCheck.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when generated TypeScript files may be outdated"
        },
        "zodra.staleCheck.intervalMs": {
          "type": "number",
          "default": 60000,
          "description": "How often to check for stale files (milliseconds)"
        }
      }
    },
    "snippets": [
      {
        "language": "ruby",
        "path": "./snippets/zodra.code-snippets"
      }
    ]
  },
  "scripts": {
    "build": "tsup src/extension.ts --format cjs --external vscode",
    "watch": "tsup src/extension.ts --format cjs --external vscode --watch",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
